# Normalize line endings: store and check out LF everywhere (Windows + Linux VPS).
* text=auto eol=lf

# Binary assets — never touch line endings.
*.png  binary
*.ico  binary
*.parquet binary
*.feather binary
*.woff  binary
*.woff2 binary
