"""Service layer: the only seam between the web layer and the engine.

Builds configs, runs simulations, persists summaries, and returns presentation-ready
data. Keeps Flask out of the engine and the engine out of Flask.

Placeholder for Stage 4/6 — see ``dev/plan-overview.md``.
"""
from __future__ import annotations
